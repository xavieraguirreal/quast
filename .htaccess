# Seguridad básica
Options -Indexes

# Proteger archivos sensibles
<FilesMatch "^(config\.php|\.htaccess)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Forzar UTF-8
AddDefaultCharset UTF-8

# Rewrite para URLs amigables (opcional)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirigir /tenant/ a listado de encuestas
    RewriteRule ^([a-z0-9-]+)/?$ lista.php?t=$1 [L,QSA]

    # Redirigir /tenant/encuesta-nombre a encuesta específica
    RewriteRule ^([a-z0-9-]+)/([a-z0-9-]+)/?$ index.php?t=$1&e=$2 [L,QSA]
</IfModule>
